# Darwin runtime + test dependencies (B1 + B2)
pydantic>=2.0
ortools>=9.0            # B1 oracle (optional at runtime; pure-Python fallback exists)
numpy
# B2 — the worker agent
google-genai>=1.40      # native Gemini provider (lazy import; only the gated integration test calls it)
motor>=3.0              # async MongoDB telemetry (lazy import; degrades to in-memory/local log)
httpx>=0.27             # OpenAI-compatible provider transport (lazy import)
# Test tooling
pytest>=8.0
pytest-cov
pytest-asyncio>=0.23
hypothesis>=6.0
mongomock
